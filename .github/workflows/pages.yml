name: Deploy GameHub to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: TypeScript validation
        run: npm run typecheck

      - name: Build static site
        run: npm run build

      - name: Verify static export
        run: |
          # Core multiplayer and sports routes.
          test -f out/pong-duel/index.html
          test -f out/hoop-duel/index.html
          test -f out/football-random/index.html
          test -f out/mini-football/index.html
          test -f out/volley-duel/index.html
          test -f out/tennis-duel/index.html
          test -f out/air-hockey/index.html
          test -f out/racing-duel/index.html

          # Current single-player landing pages. Keeping these explicit catches
          # accidental route/export regressions before a Pages deployment.
          test -f out/minesweeper/index.html
          test -f out/tap-target/index.html
          test -f out/brick-breaker/index.html
          test -f out/brick-quest/index.html
          test -f out/neon-dodge/index.html
          test -f out/bubble-pop/index.html
          test -f out/whack-attack/index.html
          test -f out/falling-blocks/index.html
          test -f out/sky-hopper/index.html
          test -f out/asteroid-blaster/index.html
          test -f out/alien-blaster/index.html

          # Static hosting metadata.
          test -f out/manifest.webmanifest
          test -f out/robots.txt
          test -f out/sitemap.xml
          test -f out/favicon.svg

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
