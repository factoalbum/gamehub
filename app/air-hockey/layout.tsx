import type { Metadata } from 'next';
export const metadata: Metadata={title:'Air Hockey Duel — 2 Player Browser Game | GameHub',description:'Play Air Hockey Duel with a friend on one device. Fast local 2-player arcade hockey, no login or download.'};
export default function Layout({children}:{children:React.ReactNode}){return children}
