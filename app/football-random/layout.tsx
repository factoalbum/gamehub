import type { ReactNode } from 'react';

export const metadata = {
  title: 'Football Random — 2 Player Game | GameHub',
  description: 'Play Football Random with a friend on one phone. One-button local multiplayer, chaotic physics and instant rematches.',
};

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
