import type {ReactNode} from 'react';
export const metadata={title:'2 Player Games — Local Multiplayer | GameHub',description:'Play GameHub local multiplayer games on one phone, tablet or computer. Fast 1P and 2P arcade games with touch controls and instant rematches.'};
export default function Layout({children}:{children:ReactNode}){return children}
