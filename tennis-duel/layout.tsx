import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Tennis Duel — 2 Player Tennis Game | GameHub', description: 'Play Tennis Duel with a friend on one device. Fast local multiplayer tennis with keyboard controls and instant rematches.' };
export default function Layout({ children }: Readonly<{children:React.ReactNode}>){return children;}
